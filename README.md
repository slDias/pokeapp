# PokeApp - A Typescript + React Pokédex app.

Install the dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

The application will be available at `http://localhost:5173`.


## Notes

- The Application is designed primarily for **mobile**. So please test it using the **Responsive Design Mode** on your browser's Developer tools.
